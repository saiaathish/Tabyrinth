import React from "react";
import { createRoot } from "react-dom/client";
import { sendMessage } from "../../platform/chrome-messaging";
import type { GameAction, GameState, Message, RoomKind } from "../../game/types";

type Intent = "enter" | "take-blade" | "restore" | "take-sigil" | "break-seal" | "attack" | "move-left" | "move-right";
const labels: Record<RoomKind, string> = { entrance: "The Threshold", armory: "The Armory", sanctum: "The Sanctum", vault: "The Vault", boss: "The Throne", void: "The Void Rift" };
const flavor: Record<RoomKind, string> = { entrance: "A quiet threshold. The tab bar is your corridor.", armory: "Old steel waits beneath a skin of dust.", sanctum: "A low, warm pulse answers your footsteps.", vault: "The sigil hums toward the throne.", boss: "The throne watches from beyond the broken seal.", void: "A cold seam has opened in the tab bar." };
const icon: Record<RoomKind, string> = { entrance: "✦", armory: "⌁", sanctum: "◉", vault: "⬡", boss: "♛", void: "∅" };

function intentMessage(intent: Intent, roomId: string, targetRoomId?: string): { type: "GAME_ACTION"; action: GameAction } {
  if (intent === "move-left" || intent === "move-right" || intent === "enter") return { type: "GAME_ACTION", action: { type: "MOVE_PLAYER", payload: { toRoomId: targetRoomId ?? roomId } } };
  const actions: Record<Exclude<Intent, "move-left" | "move-right" | "enter">, GameAction> = {
    "take-blade": { type: "TAKE_BLADE" },
    restore: { type: "MOVE_PLAYER", payload: { toRoomId: roomId } },
    "take-sigil": { type: "TAKE_SIGIL" },
    "break-seal": { type: "BREAK_SEAL" },
    attack: { type: "ATTACK_BOSS" },
  };
  return { type: "GAME_ACTION", action: actions[intent] };
}

export function RoomPage({ state, error = false, onIntent }: { state: GameState | null; error?: boolean; onIntent?: (intent: Intent, roomId: string) => void }) {
  const roomId = new URLSearchParams(location.search).get("room") ?? "";
  React.useEffect(() => { document.title = roomId === "void-rift" ? "VOID" : "TABYRINTH"; }, [roomId]);
  if (error) return <main className="room-state"><p className="eyebrow">TABYRINTH / SIGNAL LOST</p><h1>Room unavailable</h1><p>Runtime state could not be read. Keep this tab open and try again.</p></main>;
  if (!state) return <main className="room-state"><p className="eyebrow">TABYRINTH / LOADING</p><h1>Mapping the corridor…</h1><p>Waiting for the managed tab topology.</p></main>;
  const room = state.roomById[roomId];
  if (!room || room.destroyed) return <main className="room-state"><p className="eyebrow">TABYRINTH / EMPTY</p><h1>Room destroyed</h1><p>This tab no longer belongs to the active run.</p></main>;
  const index = state.orderedRoomIds.indexOf(roomId);
  const left = index > 0 ? state.orderedRoomIds[index - 1] : null;
  const right = index >= 0 && index < state.orderedRoomIds.length - 1 ? state.orderedRoomIds[index + 1] : null;
  const send = (intent: Intent) => { onIntent?.(intent, roomId); const target = intent === "move-left" ? left ?? undefined : intent === "move-right" ? right ?? undefined : undefined; void sendMessage(intentMessage(intent, roomId, target)).then((value) => window.dispatchEvent(new CustomEvent("tabyrinth-state", { detail: value }))); };
  const action = room.kind === "entrance" ? "enter" : room.kind === "armory" ? "take-blade" : room.kind === "sanctum" ? "restore" : room.kind === "vault" ? "take-sigil" : room.kind === "boss" ? (state.boss.shieldBroken ? "attack" : "break-seal") : "enter";
  const actionLabel: Record<string, string> = { enter: "Enter the dungeon", "take-blade": "Take the Blade", restore: "Restore health", "take-sigil": "Take the Sigil", "break-seal": "Break the Seal", attack: "Attack the Throne" };
  return <main className={`room-shell room-${room.kind}`}>
    <style>{css}</style>
    <header className="topbar"><span className="brand">TABYRINTH</span><span className="run-state">RUN {state.runId.slice(0, 8)} · {state.status.toUpperCase()}</span><span className="revision">SYNC {state.revision}</span></header>
    <section className="hud" aria-label="Player status"><span><b>HP</b> <strong aria-label={`${state.player.hp} of ${state.player.maxHp} health`}>{"♥".repeat(state.player.hp)}<i>{"♥".repeat(state.player.maxHp - state.player.hp)}</i></strong></span><span><b>HELD</b> {state.player.hasBlade ? "Blade" : state.player.hasSigil ? "Sigil" : "—"}</span></section>
    <div className="layout">
      <nav className="portal portal-left" aria-label="Left portal"><button disabled={!left} onClick={() => left && send("move-left")} aria-label={left ? `Open portal to ${left}` : "No room to the left"}>← <span>{left ? labels[state.roomById[left].kind] : "No corridor"}</span></button></nav>
      <article className="scene" aria-labelledby="room-title"><div className="scene-mark" aria-hidden="true">{icon[room.kind]}</div><p className="eyebrow">ROOM {String(index + 1).padStart(2, "0")} / {room.kind.toUpperCase()}</p><h1 id="room-title">{labels[room.kind]}</h1><p className="flavor">{flavor[room.kind]}</p>{room.kind === "boss" && <p className="alert" role="status">{state.boss.voidActive ? "A VOID RIFT HAS BREACHED THE TAB BAR." : state.boss.shieldBroken ? "THE THRONE IS VULNERABLE." : "THE SIGIL CANNOT CROSS A BROKEN CORRIDOR."}</p>}<button className="primary" onClick={() => send(action)}>{actionLabel[action]}</button></article>
      <nav className="portal portal-right" aria-label="Right portal"><button disabled={!right} onClick={() => right && send("move-right")} aria-label={right ? `Open portal to ${right}` : "No room to the right"}><span>{right ? labels[state.roomById[right].kind] : "No corridor"}</span> →</button></nav>
    </div>
    <section className="topology" aria-labelledby="topology-title"><div className="section-head"><h2 id="topology-title">Tab bar / live topology</h2><span>{state.orderedRoomIds.length} managed</span></div><ol>{state.orderedRoomIds.map((id, i) => <li key={id} className={id === roomId ? "current" : ""} aria-current={id === roomId ? "location" : undefined}><span>{String(i + 1).padStart(2, "0")}</span><b>{labels[state.roomById[id].kind]}</b></li>)}</ol></section>
    <aside className="feed" aria-label="Event feed"><div className="section-head"><h2>Field notes</h2><span>LIVE</span></div><p>↳ {state.flags.tutorialMoveCompleted ? "The corridor remembers your rearrangement." : "Drag a managed tab to rewire the dungeon."}</p><p>↳ {state.player.hasSigil ? "The sigil is in hand." : "The vault still holds the sigil."}</p></aside>
  </main>;
}

function App() { const [state, setState] = React.useState<GameState | null>(null); const [error, setError] = React.useState(false); React.useEffect(() => { const load = () => sendMessage({ type: "GET_STATE" }).then((value) => setState(value as GameState | null)).catch(() => setError(true)); const onMessage = (message: Message) => { if (message.type === "GAME_ACTION" || message.type === "START_RUN" || message.type === "RESET_RUN") load(); }; const onState = (event: Event) => { const value = (event as CustomEvent).detail; if (value && !value.error) setState(value as GameState); }; const onChanged = (changes: Record<string, chrome.storage.StorageChange>, area: string) => { if (area === "session" && changes.tabyrinth?.newValue) load(); }; load(); chrome.runtime.onMessage.addListener(onMessage); chrome.storage.onChanged.addListener(onChanged); window.addEventListener("tabyrinth-state", onState); return () => { chrome.runtime.onMessage.removeListener(onMessage); chrome.storage.onChanged.removeListener(onChanged); window.removeEventListener("tabyrinth-state", onState); }; }, []); return <RoomPage state={state} error={error} />; }
const css = `:root{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;color:#eee8dc;background:#121313}*{box-sizing:border-box}body{margin:0;min-width:320px}.room-shell{min-height:100vh;padding:18px clamp(18px,5vw,72px) 42px;background:radial-gradient(circle at 50% 42%,#292725 0,#121313 58%);letter-spacing:.02em}.room-shell:before{content:"";position:fixed;inset:0;pointer-events:none;opacity:.12;background-image:linear-gradient(#fff1 1px,transparent 1px),linear-gradient(90deg,#fff1 1px,transparent 1px);background-size:48px 48px}.topbar,.hud,.section-head{display:flex;justify-content:space-between;align-items:center;gap:16px}.topbar{border-bottom:1px solid #ffffff25;padding-bottom:14px;font-size:11px;color:#a9a196}.brand{color:#f3c969;font-weight:800;letter-spacing:.18em}.revision{color:#79bda7}.hud{max-width:720px;margin:24px auto 0;border:1px solid #ffffff25;padding:12px 16px;color:#c8c0b2;font-size:11px}.hud b{color:#817a70;margin-right:8px}.hud strong{color:#ed8d70;letter-spacing:5px}.hud i{font-style:normal;color:#454341}.layout{display:grid;grid-template-columns:minmax(120px,1fr) minmax(220px,620px) minmax(120px,1fr);align-items:center;gap:20px;min-height:390px}.portal{text-align:center}.portal button{border:0;background:transparent;color:#eee8dc;cursor:pointer;font:inherit;padding:16px}.portal button:hover:not(:disabled){color:#f3c969}.portal button:focus-visible,.primary:focus-visible{outline:2px solid #f3c969;outline-offset:4px}.portal button:disabled{color:#454341;cursor:not-allowed}.portal span{display:block;font-size:10px;color:#898278;margin-top:8px}.scene{text-align:center;padding:30px 20px}.scene-mark{font-family:Georgia,serif;color:#f3c969;font-size:72px;line-height:1;text-shadow:0 0 40px #d1904055}.eyebrow{color:#9f8061;font-size:10px;letter-spacing:.2em}.scene h1{font:500 clamp(30px,5vw,58px)/1 Georgia,serif;margin:16px 0}.flavor{color:#b1aaa0;font:16px/1.6 Georgia,serif;max-width:440px;margin:0 auto 26px}.alert{color:#ed8d70;font-size:11px;line-height:1.6}.primary{border:1px solid #f3c969;background:#f3c969;color:#151515;padding:14px 22px;font:700 11px inherit;cursor:pointer}.topology,.feed{max-width:900px;margin:10px auto 0;border-top:1px solid #ffffff25;padding-top:16px}.section-head h2{font-size:11px;letter-spacing:.12em;text-transform:uppercase;margin:0}.section-head span{font-size:10px;color:#817a70}.topology ol{display:flex;gap:8px;list-style:none;padding:14px 0 0;margin:0;overflow:auto}.topology li{min-width:112px;border:1px solid #ffffff20;padding:12px;color:#827c73}.topology li.current{border-color:#f3c969;color:#f3c969;background:#f3c9690d}.topology li span{display:block;font-size:10px;margin-bottom:8px}.topology li b{font-size:11px;font-weight:500}.feed{max-width:900px;margin-top:26px}.feed p{font-size:11px;color:#9f9990;margin:10px 0}.room-state{max-width:600px;margin:18vh auto;padding:32px}.room-state h1{font:48px Georgia,serif}.room-state p:not(.eyebrow){color:#aaa;line-height:1.6}@media(max-width:640px){.layout{grid-template-columns:1fr;min-height:0}.scene{order:1}.portal-left{order:2}.portal-right{order:3}.portal button{width:100%;border:1px solid #ffffff20}.topbar .run-state{display:none}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important}}`;

createRoot(document.getElementById("root")!).render(<App />);
